export async function sendMessage2(validatedContact, message) {
let SendReport={};
    // console.log(`The validated contact for the message is ${parseInt(validatedContact)}.
    // The message is here${message}`)
    return SendReport={
        from:"971589969886",
        to: validatedContact
    };
}